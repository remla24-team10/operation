version: '1'
services:
  app:
    build: ./app
    image: app
    ports:
      - "5000:5000"
    networks:
      - mynetwork
    volumes:
      - ./app:/app
  model-service:
    build: ./model-service
    image: model-service
    ports:
      - "8080:8080"
    networks:
      - mynetwork
    volumes:
      - ./model-service/model_service:/model-service/model_service
networks:
  mynetwork:
    driver: bridge
